<%= form_with(model: cocktail, local: false) do |form| %>
  <% if cocktail.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cocktail.errors.count, "error") %> prohibited this cocktail from being saved:</h2>

      <ul>
        <% cocktail.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.hidden_field :user_id %>
    <%= hidden_field_tag :user_id, value: current_user %>
  </div>
  <% if @bar_id.present? %>
    
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.collection_select :ingredient_id, Ingredient.all, :id, :name, prompt: true %>
  </div>

  <div class="field">
    <%= form.label :recipe %>
    <%= form.text_area :recipe %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
